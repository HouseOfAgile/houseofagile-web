# House of Agile Email Server Configuration
# Copy this file to .env and fill in your actual values

# SMTP Configuration for sending emails
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Email credentials (use App Password for Gmail)
SENDER_EMAIL=your-email@gmail.com
SENDER_PASSWORD=your-app-password-here
RECIPIENT_EMAIL=jc@houseofagile.com

# For Gmail users:
# 1. Enable 2FA on your Google account
# 2. Go to Google Account settings > Security > App passwords
# 3. Generate an app password for 'Mail'
# 4. Use that 16-character password in SENDER_PASSWORD

# Alternative SMTP providers:
#
# For Outlook/Hotmail:
# SMTP_SERVER=smtp.live.com
# SMTP_PORT=587
#
# For Yahoo:
# SMTP_SERVER=smtp.mail.yahoo.com
# SMTP_PORT=587
#
# For custom SMTP (like Mail-in-a-Box):
# SMTP_SERVER=mail.yourdomain.com
# SMTP_PORT=587